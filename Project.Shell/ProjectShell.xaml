<Window x:Class="Project.Shell.ProjectShell"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project.Shell"
             xmlns:prism="http://www.codeplex.com/prism"
             mc:Ignorable="d" 
             Height="Auto" Width="Auto">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" prism:RegionManager.RegionName="HeaderView"/>
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="MainAreaView"/>
        <ContentControl Grid.Row="2" prism:RegionManager.RegionName="FooterView"/>
    </Grid>
</Window>
